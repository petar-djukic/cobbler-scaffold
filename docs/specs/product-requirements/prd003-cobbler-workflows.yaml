id: prd003-cobbler-workflows
title: Cobbler Measure and Stitch Workflows

implemented_by:
  - Measure Workflow
  - Stitch Workflow
  - Claude Runner
  - Issue Tracking
  - Claude Execution
  - Prompt Templates
  - Context Assembly
used_by:
  - Git Operations

problem: |
  Turning project specifications into working code requires two distinct
  capabilities: analyzing what needs to be built and executing individual
  tasks. Without structured workflows for these phases, the orchestrator
  cannot decompose work into manageable pieces or execute them with
  proper isolation, metrics tracking, and error recovery.

goals:
  - G1: Define the measure workflow that proposes tasks from project state
  - G2: Define the stitch workflow that executes tasks in isolated worktrees
  - G3: Support configurable Claude invocation parameters
  - G4: Record invocation metrics for every Claude session
  - G5: Handle task dependencies and lifecycle through GitHub Issues integration

requirements:
  R1:
    title: Workflow Configuration
    items:
      - R1.1: Measure and stitch must read SilenceAgent, MaxStitchIssuesPerCycle, MaxMeasureIssues, UserPrompt, GenerationBranch, and TokenFile from Config (see prd001 R1.16-R1.23)
      - R1.2: Measure and stitch must read ClaudeArgs from Config for Claude invocation arguments
      - R1.3: RunMeasure and RunStitch must take no parameters; all options come from Config

  R2:
    title: Measure Workflow
    items:
      - R2.1: Measure must verify the GitHub Issues repo is reachable before proceeding
      - R2.2: Measure must resolve and switch to the target branch
      - R2.3: Measure must query existing GitHub Issues from the configured issues repo via gh issue list
      - R2.4: Measure must clean up old proposed-issues files from the cobbler directory
      - R2.5: Measure must build the prompt from the template with existing issues, limit, output path, and user input
      - R2.6: Measure must invoke Claude with the built prompt
      - R2.7: Measure must parse the YAML output file written by Claude
      - R2.8: Measure must create proposed issues as GitHub Issues with title, description, and generation label via gh issue create
      - R2.9: Measure must wire dependencies between imported issues based on the dependency index
      - R2.10: Measure must record an InvocationRecord on each created issue
      - R2.11: Reserved (GitHub Issues are created remotely; no local commit required)

  R3:
    title: Stitch Workflow
    items:
      - R3.1: Stitch must verify the GitHub Issues repo is reachable before proceeding
      - R3.2: Stitch must resolve and switch to the target branch
      - R3.3: Stitch must recover stale tasks from interrupted runs before processing
      - R3.4: Stitch must pick ready tasks one at a time from GitHub Issues (open issues with the generation label and without the cobbler-in-progress label)
      - R3.5: Stitch must stop when no ready tasks remain or MaxIssues is reached
      - R3.6: For each task, stitch must claim the task by setting status to in_progress
      - R3.7: For each task, stitch must create a git worktree on a task branch
      - R3.8: Task branches must follow the pattern task/{baseBranch}-{issueID}
      - R3.9: For each task, stitch must capture LOC before Claude
      - R3.10: For each task, stitch must build the prompt from the template with task data
      - R3.11: For each task, stitch must invoke Claude in the worktree directory
      - R3.12: The orchestrator must stage and commit Claude's changes in the worktree (Claude is forbidden from running git commands)
      - R3.13: For each task, stitch must merge the task branch back to the base branch
      - R3.14: For each task, stitch must capture LOC after Claude and git diff stats
      - R3.15: For each task, stitch must clean up the worktree and delete the task branch
      - R3.16: For each task, stitch must record an InvocationRecord and close the task
      - R3.17: worktreeBasePath must derive the repo name from git rev-parse --git-common-dir so the temp directory for task worktrees is identical whether stitch is invoked from the main repo root or from a git worktree; it must fall back to filepath.Base(os.Getwd()) if git is unavailable

  R4:
    title: Recovery
    items:
      - R4.1: recoverStaleTasks must scan for stale task branches matching the pattern
      - R4.2: recoverStaleTasks must remove orphaned worktrees and delete stale branches
      - R4.3: recoverStaleTasks must reset affected issues to ready status
      - R4.4: resetOrphanedIssues must find in_progress issues without corresponding task branches
      - R4.5: resetOrphanedIssues must reset orphaned issues to ready status
      - R4.6: Recovery must remove the cobbler-in-progress label from affected GitHub Issues if any state was recovered
      - R4.7: recoverStaleTasks and GeneratorResume must find stale task worktrees regardless of whether they are invoked from the main repo root or from a git worktree of the same repository

  R5:
    title: Prompt Templates
    items:
      - R5.1: Default measure template must be embedded via go:embed from pkg/orchestrator/prompts/measure.tmpl
      - R5.2: Default stitch template must be embedded via go:embed from pkg/orchestrator/prompts/stitch.tmpl
      - R5.3: Custom templates from Config must override the embedded defaults
      - R5.4: MeasurePromptDoc must provide ProjectContext (YAML blob of all docs, specs, source code, and issues), a Task field, Constraints, and OutputFormat assembled from the template and Config
      - R5.5: StitchPromptDoc must provide Title, ID, IssueType, Description, and ProjectContext (built from worktree)
      - R5.6: MeasurePromptDoc must include LinesMin and LinesMax from Config.EstimatedLinesMin and Config.EstimatedLinesMax embedded in the task text
      - R5.7: MeasurePromptDoc must include PlanningConstitution and IssueFormatConstitution (embedded or from Config overrides) as yaml.Node fields
      - R5.8: StitchPromptDoc must include ExecutionConstitution and GoStyleConstitution (embedded or from Config overrides) as yaml.Node fields
      - R5.9: MeasurePromptDoc must include GoldenExample (from Config.Cobbler.GoldenExample) when set, injected into the measure prompt to guide Claude's issue format and granularity

  R6:
    title: Claude Invocation
    items:
      - R6.1: runClaude must execute the claude binary with the prompt on stdin
      - R6.2: Reserved (container mounting removed — podman support dropped)
      - R6.3: Reserved (container working directory removed — podman support dropped)
      - R6.4: runClaude must capture stdout for token parsing
      - R6.5: parseClaudeTokens must extract input tokens, output tokens, cache creation tokens, cache read tokens, and cost from stream-json output
      - R6.6: When silence is false, Claude stdout must be tee'd to both buffer and os.Stdout
      - R6.7: runClaude must pass Config.ClaudeArgs to the claude binary
      - R6.8: runClaude must use context.WithTimeout to enforce ClaudeMaxTimeSec; process is killed on expiry
      - R6.10: On Claude failure or timeout in stitch, the task must be reset to ready and the worktree cleaned up
      - R6.11: On merge failure in stitch, the task must be reset to ready and the worktree cleaned up
      - R6.12: runClaude must refresh credentials from macOS Keychain before each invocation
      - R6.13: runClaude must capture RawOutput (full stream-json bytes) for history saving

  R7:
    title: Issue Import and Validation
    items:
      - R7.1: proposedIssue must include index (int), title (string), description (string), and dependency (int)
      - R7.2: importIssues must create a GitHub Issue for each proposed issue via gh issue create with the generation label
      - R7.3: importIssues must order issues by dependency index so dependent issues are created after their prerequisites
      - R7.4: Reserved (GitHub Issues are created remotely; no local commit required)
      - R7.5: When Config.Cobbler.MaxRequirementsPerTask is greater than zero, measure must count the number of requirement items in each proposed issue's description and reject issues that exceed the limit; rejected issues must trigger a re-prompt (up to Config.Cobbler.MaxMeasureRetries attempts) instructing Claude to split them into smaller tasks
      - R7.6: When Config.Cobbler.MaxRequirementsPerTask is greater than zero and Config.Cobbler.MaxWeightPerTask is zero, applyDefaults must set MaxWeightPerTask equal to MaxRequirementsPerTask so that weight-based validation is always active when count-based batching is configured (GH-1951)
      - R7.7: checkAutoAdvanceRelease must not advance a release when any requirement from PRDs referenced by the release's UC touchpoints is still ready in requirements.yaml, even if all UCs have a done status; this prevents orphaned ready requirements in uncited R-groups from becoming invisible to the measure agent (GH-1952)

  R8:
    title: Pre-flight Checks
    items:
      - R8.1: Reserved (checkPodman removed — podman support dropped)
      - R8.2: Reserved (checkPodman removed — podman support dropped)
      - R8.3: Reserved (checkPodman removed — podman support dropped)
      - R8.4: Reserved (checkPodman removed — podman support dropped)
      - R8.5: Reserved (checkPodman removed — podman support dropped)
      - R8.6: Reserved (checkPodman removed — podman support dropped)

  R9:
    title: Phase-Specific Context Files
    items:
      - R9.1: The orchestrator must define a PhaseContext struct with include, exclude, sources, and release fields
      - R9.2: loadPhaseContext must read a YAML file at the given path and return a parsed PhaseContext
      - R9.3: loadPhaseContext must return (nil, nil) when the file does not exist
      - R9.4: loadPhaseContext must return an error when the file exists but is malformed
      - R9.5: buildProjectContext must accept an optional *PhaseContext parameter
      - R9.6: When PhaseContext is non-nil, its non-empty fields must override the corresponding ProjectConfig fields
      - R9.7: When PhaseContext is nil, buildProjectContext must use ProjectConfig fields unchanged
      - R9.8: buildMeasurePrompt must load {CobblerConfig.Dir}/measure_context.yaml at invocation time and pass it to buildProjectContext
      - R9.9: buildStitchPrompt must load {CobblerConfig.Dir}/stitch_context.yaml at invocation time and pass it to buildProjectContext
      - R9.10: The orchestrator must log which context file was loaded or that fallback to Config was used

  R10:
    title: Pre-Cycle Analysis
    items:
      - R10.1: Before each measure/stitch cycle, the orchestrator must run RunPreCycleAnalysis() to produce a combined consistency and code-status snapshot
      - R10.2: RunPreCycleAnalysis must invoke the cross-artifact consistency checker (see prd001 R8) and collect its results into an AnalysisDoc
      - R10.3: RunPreCycleAnalysis must invoke CodeStatus (see prd001 R7) and include the report in the same AnalysisDoc
      - R10.4: RunPreCycleAnalysis must write the AnalysisDoc as YAML to {CobblerConfig.Dir}/analysis.yaml
      - R10.5: loadAnalysisDoc must load {CobblerConfig.Dir}/analysis.yaml and return nil when the file does not exist
      - R10.6: buildProjectContext must include the AnalysisDoc content when analysis.yaml is present so Claude sees current project health in both measure and stitch prompts
      - R10.7: Errors during RunPreCycleAnalysis must be logged and must not block the cycle; the analysis is advisory

  R11:
    title: Defect Routing
    items:
      - R11.1: AnalysisDoc must include a Defects []string field that holds schema errors and constitution drift findings from AnalyzeResult; these must be separate from ConsistencyDetails
      - R11.2: RunPreCycleAnalysis must populate Defects from AnalyzeResult.SchemaErrors and AnalyzeResult.ConstitutionDrift, and must exclude those findings from ConsistencyDetails
      - R11.3: RunMeasure must read Defects from the AnalysisDoc after RunPreCycleAnalysis and file each defect as a GitHub bug issue in the target repo
      - R11.4: The target repo is resolved by checking project.target_repo in Config first; if empty, by stripping "github.com/" from project.module_path; if neither yields a non-empty value, defect filing is skipped with a log warning
      - R11.5: Filed defect issues must use the "bug" label and must include the defect detail string (which already contains the file path and error) in the issue body
      - R11.6: Defect filing is best-effort; errors from gh issue create are logged and must not block the measure cycle
      - R11.7: Defects must not be passed to Claude in the measure prompt; the Defects field in AnalysisDoc must be omitted from ConsistencyDetails so Claude does not see or act on them

  R12:
    title: Source Summarization Modes
    items:
      - R12.1: "CobblerConfig must include a MeasureSourceMode string field with three valid values: full (default, include source verbatim), headers (exported declarations only, no function bodies), and custom (run a configured command per file and use its stdout as the summarized content)."
      - R12.2: "CobblerConfig must include a MeasureSummarizeCommand string field used when MeasureSourceMode is custom; the command receives the source file path as its last argument and must write the summary to stdout."
      - R12.3: "headers mode must produce output containing the package declaration, the import block, and all exported type, function, constant, and variable declarations with their doc comments; function and method bodies must be omitted."
      - R12.4: "custom mode must run MeasureSummarizeCommand with the source file path appended; on non-zero exit or empty stdout the orchestrator must fall back to full content for that file and log a warning."
      - R12.5: "The stitch prompt must always use full source content regardless of MeasureSourceMode; summarization applies only during buildMeasurePrompt."
      - R12.6: "PhaseContext must include SourceMode string and SummarizeCommand string fields so that per-invocation context files can override the config-level mode; when PhaseContext.SourceMode is non-empty it takes precedence over CobblerConfig.MeasureSourceMode."
      - R12.7: "When MeasureSourceMode is empty or full, buildProjectContext must produce output identical to the current implementation; no existing tests may fail."

non_goals:
  - This PRD does not define the generation lifecycle (see prd002)
  - This PRD does not define the prompt content or Claude behavior expectations
  - This PRD does not define concurrent task execution (tasks run sequentially)

acceptance_criteria:
  - id: AC1
    criterion: Measure proposes tasks from project state and creates them as GitHub Issues
    traces:
      - R1.1
      - R1.2
      - R1.3
      - R2.1
      - R2.2
      - R2.3
      - R2.4
      - R2.5
      - R2.6
      - R2.7
      - R2.8
      - R2.9
      - R2.10
      - R2.11
  - id: AC2
    criterion: Stitch executes ready tasks in isolated worktrees and merges results
    traces:
      - R3.1
      - R3.2
      - R3.3
      - R3.4
      - R3.5
      - R3.6
      - R3.7
      - R3.8
      - R3.9
      - R3.10
      - R3.11
      - R3.12
      - R3.13
      - R3.14
      - R3.15
      - R3.16
      - R3.17
  - id: AC3
    criterion: Recovery resets stale tasks and orphaned issues to ready
    traces:
      - R4.1
      - R4.2
      - R4.3
      - R4.4
      - R4.5
      - R4.6
      - R4.7
  - id: AC4
    criterion: Custom prompt templates override embedded defaults
    traces:
      - R5.1
      - R5.2
      - R5.3
      - R5.4
      - R5.5
      - R5.6
      - R5.7
      - R5.8
      - R5.9
  - id: AC5
    criterion: Claude is invoked with configurable arguments and enforced timeout
    traces:
      - R6.1
      - R6.2
      - R6.3
      - R6.4
      - R6.5
      - R6.6
      - R6.7
      - R6.8
      - R6.10
      - R6.11
      - R6.12
      - R6.13
  - id: AC6
    criterion: Proposed issues are validated and imported into GitHub with correct dependencies
    traces:
      - R7.1
      - R7.2
      - R7.3
      - R7.4
      - R7.5
      - R7.6
      - R7.7
  - id: AC7
    criterion: Reserved (pre-flight podman checks removed — podman support dropped)
    traces:
      - R8.1
      - R8.2
      - R8.3
      - R8.4
      - R8.5
      - R8.6
  - id: AC8
    criterion: Measure and stitch each read their own context file at invocation time when present
    traces:
      - R9.1
      - R9.2
      - R9.3
      - R9.4
      - R9.5
      - R9.6
      - R9.7
      - R9.8
      - R9.9
      - R9.10
  - id: AC9
    criterion: Projects without context files use current behavior unchanged
    traces:
      - R9.3
      - R9.7
  - id: AC10
    criterion: Pre-cycle analysis produces a combined consistency and code-status snapshot before each cycle
    traces:
      - R10.1
      - R10.2
      - R10.3
      - R10.4
      - R10.5
      - R10.6
      - R10.7
  - id: AC11
    criterion: Schema errors and constitution drift are filed as bug issues in the target repo and excluded from the measure prompt
    traces:
      - R11.1
      - R11.2
      - R11.3
      - R11.4
      - R11.5
      - R11.6
      - R11.7
  - id: AC12
    criterion: Source summarization mode is configurable per project; headers and custom modes reduce measure prompt size without affecting stitch
    traces:
      - R12.1
      - R12.2
      - R12.3
      - R12.4
      - R12.5
      - R12.6
      - R12.7
